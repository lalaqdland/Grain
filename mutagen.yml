sync:
  defaults:
    mode: two-way-resolved
    ignore:
      paths:
        - "/.git"
        - "/frontend/node_modules"
        - "/frontend/.next"
        - "/backend/__pycache__"
        - "/storage/temp"
        - "/models"
        - "/backend/.env"

  code:
    alpha: "."
    beta: "aliyun:/opt/gain-project"
    mode: two-way-resolved
