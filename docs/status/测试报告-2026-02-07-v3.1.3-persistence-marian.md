# 测试报告（v3.1.3：持久化 + Marian 全启用）

- 日期：2026-02-07
- 分支：`dev`
- 目标：完成 v3.1.3 的 P0/P1 本地实现与回归

## 1. 变更范围

- `GET /api/v1/export/stats` 从内存聚合升级为 SQLite 持久化
- `window_minutes` 上限提升至 `43200`
- Marian 默认启用 + Compose 全环境显式 `USE_MARIAN_MT=true`
- 预览前端 `next dev` 健康检查调高容错参数
- 新增 Marian `used` / `generation_failed` API 回归用例

## 2. 自动化测试结果

### 后端（pytest）

命令：

```bash
python -m pytest -q
```

结果：

- `23 passed in 1.40s`
- 新增覆盖：
  - SQLite 统计跨实例持久化
  - `window_minutes=43200/43201` 边界
  - Marian `used` 与 `generation_failed` API 级路径

### 前端（构建 + E2E）

命令：

```bash
cd frontend
npm run build
npm run e2e
```

结果：

- `npm run build`：通过
- `npm run e2e`：`1 passed`

备注：

- 构建首次出现 `.next` 缓存态异常（`/_document`），清理缓存目录后复测通过。

## 3. 接口级结论

- ✅ `GET /api/v1/export/stats?window_minutes=43200` 可用
- ✅ `GET /api/v1/export/stats?window_minutes=43201` 返回 `422`
- ✅ `POST /api/v1/rewrite` 可观测 `diagnostics.marian.status=used`
- ✅ Marian 异常时可观测 `diagnostics.marian.status=generation_failed`，接口整体仍返回成功并保留 DeepSeek 候选

## 4. 待完成项

- ⏳ 本轮未执行远端 dev 预览环境部署与线上验收。
- ⏳ 需在下一轮完成 `/opt/gain-preview` 重建后验证：
  - `api/health` 版本号
  - `api/v1/info` 的 Marian 运行态
  - `export/stats` 重启后持久化

## 5. 结论

- ✅ v3.1.3 本地实现与自动化回归通过。
- ⚠️ 线上（dev）验收尚未执行，发布前需补齐远端验证。
