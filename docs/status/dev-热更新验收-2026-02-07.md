# dev 预览环境热更新验收（2026-02-07）

> 历史快照：该报告记录 v3.1.1 阶段的 dev 同步与验收结果。  
> v3.1.2 版本尚未在本轮执行 dev 远端热更新。

## 本次目标

- 将当前本地工作区（含未提交改动）同步到 `dev.grain.capoo.tech`
- 验证核心闭环：上传 -> 改写 -> 导出
- 验证新接口行为：`unit/option_count` 与导出 `failed_ids`

## 执行过程

1. 本机未检测到 `mutagen` 可执行文件（命令不存在）。
2. 采用等效同步方案：
   - 本地打包当前工作区（排除 `.git`、缓存、`backend/.env`）
   - `scp` 上传到 `/opt/gain-preview/.deploy-preview.tar`
   - 服务器解包并执行 `docker compose -f docker-compose.preview.yml up -d --build`
3. 首轮重建后后端 502，日志定位为旧环境变量 `marian_model_path` 导致配置校验失败。
4. 本地补充兼容后再次同步重建，API恢复正常。

## 关键验证结果

### 环境级

- `https://dev.grain.capoo.tech` -> `200 OK`
- `https://dev.grain.capoo.tech/api/health` -> `{"status":"healthy"...}`
- `https://dev.grain.capoo.tech/api/v1/info` -> 包含 `sentence_rewrite: true`

### 接口级

- 上传：`POST /api/v1/upload` -> `200`
  - `doc_id`: `doc_34bdac5de0a3`
  - `first_para_id`: `para_000000`
- 句子改单元：`POST /api/v1/rewrite`
  - 请求：`unit=sentence`, `option_count=2`
  - 响应：`status=200`, `option_count=2`, `unit=sentence`
- 导出无效ID：`POST /api/v1/export`
  - 响应：`400`
  - `detail.failed_ids = ["para_not_exist"]`

### 业务级

- 导出有效替换：`POST /api/v1/export`
  - 使用 `para_000000 -> DEV_E2E_EXPORT_MARKER_20260207`
  - 响应：`200`
  - 导出文档首段文本命中 marker（写回成功）

## 结论

- ✅ dev 预览已更新到本地当前实现。
- ✅ 核心闭环可用，且“导出静默失败”已转为可观测错误。
- ✅ 句子/段落辅助改写与导出一致性在 dev 环境通过验收。

## 备注

- 本次未触碰生产环境。
- 若后续需恢复 Mutagen 持续热同步，需在本机安装并配置 `mutagen` 命令。

