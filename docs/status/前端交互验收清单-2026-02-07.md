# 前端交互验收清单（2026-02-07）

> 结论口径：本清单已按 v3.1.2 本地验收结果更新。

## 上传与展示

- [x] 上传 `.docx` 后成功展示段落列表
- [x] 上传非 `.docx` 返回友好错误提示

## 改写交互

- [x] 段落悬停可触发 `改段`
- [x] 选中句子后出现“已选中句子”面板
- [x] 点击 `生成句子候选` 可返回 2-3 个候选
- [x] 候选支持显示来源（`deepseek` / `marian`）
- [x] 重复句场景按选区 offset 精确替换（不会误替换首个同句）

## 结果应用

- [x] 选择段落候选后整段替换并标记已修改
- [x] 选择句子候选后仅替换目标句并标记已修改

## 导出一致性

- [x] 导出走 `POST /api/v1/export`
- [x] 导出请求仅包含已修改段落 `modifications`
- [x] 导出的 `.docx` 文本与页面替换结果一致
- [x] 导出遇到无效段落ID时前端能提示 `failed_ids`
