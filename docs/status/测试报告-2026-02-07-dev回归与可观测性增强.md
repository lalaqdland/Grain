# 测试报告（dev 回归与可观测性增强）

- 日期：2026-02-07
- 分支：`dev`
- 目标：完成 v3.1.2-observability 的 P0+P1 验收

## 1. 自动化测试结果

### 后端（pytest）

命令：

```bash
python -m pytest -q
```

结果：

- `19 passed in 0.47s`
- 新增覆盖点：
  - 导出失败统计聚合（时间窗、`doc_id` 过滤、`top_n`）
  - `GET /api/v1/export/stats` 接口行为
  - `POST /api/v1/rewrite` 的 Marian 诊断状态分支
  - `GET /api/v1/info` 运行态字段（`runtime.marian`）

### 前端（构建 + E2E）

命令：

```bash
cd frontend
npm run build
npm run e2e
```

结果：

- `npm run build`：通过
- `npm run e2e`：`1 passed`

## 2. dev 预览环境回归

### 部署动作（仅预览目录）

```bash
mutagen sync flush code
ssh capoo-hk "cd /opt/gain-preview && docker compose -f docker-compose.preview.yml up -d --build --force-recreate"
```

### 核心检查

- `https://dev.grain.capoo.tech/api/health` -> `{"status":"healthy","service":"Grain API","version":"3.1.2"}`
- `https://dev.grain.capoo.tech/api/v1/info` -> 包含 `runtime.marian`
- 闭环验证（真实请求）：
  - 上传：`POST /api/v1/upload` -> `200`
  - 改写：`POST /api/v1/rewrite` -> `200`，`diagnostics.marian.status=disabled`
  - 无效导出：`POST /api/v1/export` -> `400 + failed_ids`
  - 统计查询：`GET /api/v1/export/stats?window_minutes=60&doc_id=<doc_id>` -> 命中 `failed_requests=1`

## 3. 结论

- ✅ P0：Mutagen 热同步恢复 + dev 预览回归通过。
- ✅ P1：`failed_ids` 可观测性与 Marian 运行态可观测性已落地并验证。
- ✅ 接口保持向后兼容（新增字段均为增量）。

## 4. 风险与备注

- 统计为内存态，服务重启后清空。
- 预览前端运行 `next dev`，healthcheck（5s）在编译期间可能误报 `unhealthy`，但页面和 API 可访问。
- 本轮未触碰生产目录 `/opt/gain-project`。

