# 测试报告（Offset替换与E2E）

- 日期：2026-02-07
- 分支：`dev`
- 版本：`v3.1.2`
- 目标：
  - 修复重复句场景的句子误替换（首次匹配 -> offset 精确替换）
  - 新增前端 Playwright 半真实 E2E（上传/导出真实，rewrite mock）
  - 修复 `GET /api/v1/upload/documents/{doc_id}` 501

## 1. 自动化测试结果

### 1.1 后端（pytest）

命令：

```bash
python -m pytest -q
```

结果：

- `11 passed in 0.39s`
- 新增覆盖点：
  - `GET /api/v1/upload/documents/{doc_id}` 成功返回
  - `GET /api/v1/upload/documents/{doc_id}` 对不存在文档返回 404

### 1.2 前端构建（Next.js）

命令：

```bash
cd frontend
npm run build
```

结果：

- 构建成功
- TypeScript 检查通过

### 1.3 前端 E2E（Playwright）

命令：

```bash
cd frontend
npm run e2e
```

结果：

- `1 passed`
- 覆盖流程：上传 -> 选句（第二个重复句）-> 生成候选 -> 选择候选 -> 导出

## 2. 功能性结论

- ✅ 句子替换改为 offset 精确替换，重复句不会误替换首个命中。
- ✅ 句子候选 key 含 offset，避免同段不同选区候选串扰。
- ✅ `GET /api/v1/upload/documents/{doc_id}` 已可用，不再返回 501。
- ✅ E2E 已验证导出请求 `modifications` 与页面最终文本一致。

## 3. 风险与说明

1. E2E 首次运行需安装浏览器：`npx playwright install chromium`。
2. 本轮未执行 dev 远端热更新（Mutagen 恢复仍待后续处理）。
3. Next.js dev 环境提示未来需配置 `allowedDevOrigins`，当前不影响用例通过。

## 4. 建议后续

1. 在 dev 预览环境复跑一遍同链路验收并落库报告。
2. 为导出失败 `failed_ids` 增加频次统计和告警阈值。
3. 增加 Marian 依赖探测结果输出，提升候选来源可观测性。
