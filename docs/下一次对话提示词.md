# 下一次对话提示词

## 1. 文档维护规则（短）

- 每次开发会话结束必须更新本文件。
- 本文件只保留**当前真实状态**和**下一步可执行任务**，禁止保留过时 Sprint 模板。
- 若状态有变更（接口、部署、测试结果），同步更新：
  - `docs/API文档.md`
  - `docs/开发日志.md`
  - `docs/status/*.md`

---

## 2. 当前真实状态（v3.1.2，P0闭环完成）

- 代码分支：`dev`
- 本轮已完成：
  - 句子替换由首次匹配升级为 **selection offset 精确替换**（支持重复句场景）
  - 新增 `GET /api/v1/upload/documents/{doc_id}`（原 501 改为可用查询）
  - 前端 Playwright E2E（半真实链路）：上传 -> 选句 -> 候选 -> 选择 -> 导出
  - 文档全量巡检并补充最新状态报告
- 自动化验证：
  - `python -m pytest -q` -> `11 passed`
  - `frontend npm run build` -> 通过
  - `frontend npm run e2e` -> `1 passed`
- 预览环境：
  - 本轮未进行 dev 远端热更新操作（仅本地实现与验收）

---

## 3. 当前阻塞/风险

1. 本机尚未恢复 `mutagen`，dev 持续热同步仍依赖手工等效流程。
2. 导出 `failed_ids` 目前仅返回给调用方，尚未做统计聚合。
3. Marian 为可选链路；服务器未装 `transformers/torch/sentencepiece` 时会自动回退 DeepSeek。
4. E2E 首次执行需先安装 Playwright 浏览器：`npx playwright install chromium`。

---

## 4. 下一步优先任务（P0/P1/P2）

### P0（必须优先）

1. dev 热同步恢复
   - 本机安装 Mutagen，并恢复 `mutagen project start`。
2. 预览环境回归验收
   - 将 v3.1.2 推到 `dev.grain.capootech.com` 并复跑闭环检查。

### P1（重要）

1. 导出可观测性
   - 统计 `POST /api/v1/export` 的 `failed_ids` 频次（按 doc_id/时间窗聚合）。
2. Marian 链路健康提示
   - 增加依赖探测结果输出，明确候选来源与回退原因。

### P2（可延后）

1. 前端交互细化
   - 选区无效提示分级（跨段、空选区、快照漂移）
2. 文档结构继续收敛
   - 持续减少重复说明，保留单一事实源

---

## 5. 可复用提示词模板（下一轮工程任务）

```text
【Grain 项目 - v3.1.2 后续迭代】

当前状态：
- 句子替换已升级为 offset 精确替换，重复句场景已修复。
- API新增可用接口：GET /api/v1/upload/documents/{doc_id}
- 自动化通过：pytest 11 passed、frontend build通过、Playwright e2e 1 passed

请完成本轮任务：
1. 恢复 Mutagen 持续热同步并完成 dev 环境回归
2. 增加导出失败ID（failed_ids）统计日志
3. 增强 Marian 依赖探测与健康提示

技术约束：
- 不改生产环境
- 保持后端 API 向后兼容
- 每次改动后必须更新 docs/status 测试记录

验收标准：
- dev 预览站闭环通过
- failed_ids 有统计结果可查
- Marian 开启/回退行为可观测
```

---

## 6. 验收清单与回滚提示

### 开发完成后最小验收

- [ ] `python -m pytest -q` 全通过
- [ ] `frontend npm run build` 通过
- [ ] `frontend npm run e2e` 通过
- [ ] dev 环境 `api/health` 正常

### 回滚提示（仅预览环境）

- 优先回滚 `/opt/gain-preview` 到上一个稳定包并 `docker compose up -d --build`。
- 不要对生产目录 `/opt/gain-project` 执行任何回滚操作。
