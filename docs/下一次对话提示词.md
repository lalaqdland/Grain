# 下一次对话提示词

## 1. 文档维护规则（短）

- 每次开发会话结束必须更新本文件。
- 本文件只保留**当前真实状态**和**下一步可执行任务**。
- 若状态有变更（接口、部署、测试结果），同步更新：
  - `docs/API文档.md`
  - `docs/开发日志.md`
  - `docs/status/*.md`

---

## 2. 当前真实状态（v3.1.3）

- 代码分支：`dev`
- 本轮已完成：
  - ✅ **v3.1.4-dev**：rewrite 链路超时与降级策略
    - `deepseek_request_timeout: int = 30`（秒）
    - `deepseek_degradation_enabled: bool = True`
    - 新增 `_rewrite_with_deepseek_safe()` 包装超时/连接错误
    - 降级时返回原始文本作为 fallback
    - Git commit: `b3ac868`
  - ✅ **v3.1.4-dev**：SQLite 统计归档策略
    - `prune_old_events()` 方法
    - `POST /export/stats/prune` 接口
    - Git commit: `f24440e`
  - ✅ dev 预览环境已热部署到 `v3.1.3`（仅 `/opt/gain-preview`）
    - `api/health.version=3.1.3`
    - `api/v1/info.runtime.marian.enabled=true`
    - backend 容器环境变量 `USE_MARIAN_MT=true`
  - ✅ `export/stats` 持久化已在 dev 重启后验证（数据不丢失）
  - ✅ 新增镜像加固：`backend/.dockerignore` 排除 `.env`
  - ✅ 新增 `GET /api/v1/export/stats/storage`（SQLite 体积与风险分级）
  - ✅ 新增配置阈值：
    - `EXPORT_STATS_DB_WARN_BYTES`（默认 `10485760`）
    - `EXPORT_STATS_DB_CRITICAL_BYTES`（默认 `52428800`）
  - ✅ `docker-compose.yml` / `docker-compose.preview.yml` / `docker-compose.server.yml`
    - backend 已显式注入 `EXPORT_STATS_DB_WARN_BYTES` 与 `EXPORT_STATS_DB_CRITICAL_BYTES`
  - ✅ `runtime.marian` 新增运行态监控字段：
    - `model_loaded`
    - `first_load_duration_ms`
    - `load_attempts`
    - `load_failures`
    - `generation_attempts`
    - `generation_failures`
    - `generation_failure_rate`
    - `last_generation_error`
  - ✅ 预览上传 404 热修复
    - 原因：`NEXT_PUBLIC_API_URL=/api` 与 `'/api/v1/*'` 组合导致 `'/api/api/v1/*'`
    - 修复：前端 API base URL 归一化去重（`frontend/src/lib/api.ts`）
- 自动化验证：
  - `python -m pytest -q` -> `28 passed`
  - `frontend npm run build` -> 通过
  - `frontend npm run e2e` -> `1 passed`

---

## 3. 当前阻塞/风险

1. ~~`rewrite` 仍为单 worker 同步外部调用~~ → ✅ **v3.1.4 已添加超时与降级策略**（超时后快速返回，不长期阻塞）
2. Marian 首次模型加载耗时高，冷启动窗口期波动明显（依赖模型缓存状态）。
3. ~~`export_stats.db` 已有体积分级能力，但备份/归档自动化策略仍未落地~~ → ✅ **v3.1.4 已添加 prune 接口**

---

## 4. 下一步优先任务（P0/P1/P2）

### P1（重要）

1. ~~rewrite 链路超时与降级策略~~ ✅ v3.1.4
2. ~~持久化运维策略完善~~ ✅ v3.1.4（prune 接口）
3. 监控阈值落地
   - 基于 `runtime.marian` 和 `/export/stats/storage` 输出补齐告警阈值建议

### P2（可延后）

1. 前端交互细化
  - 选区无效提示分级（跨段、空选区、快照漂移）
2. 文档结构继续收敛
   - 减少重复说明，保持单一事实源

---

## 5. 验收清单与回滚提示

### 开发完成后最小验收

- [x] `python -m pytest -q` 全通过
- [x] `frontend npm run build` 通过
- [x] `frontend npm run e2e` 通过
- [x] dev 环境 `api/health` 正常
- [x] `api/v1/export/stats` 在重启后可查询历史统计
- [x] `api/v1/info` 显示 Marian `enabled=true` 且依赖就绪
- [x] `api/v1/export/stats/storage` 返回体积与分级

### 回滚提示

- 关闭 Marian：将 `USE_MARIAN_MT=false` 后重建相关容器。
- 统计层回滚：恢复 `backend/app/core/export_observability.py` 旧实现并去除 `/export/stats/storage` 路由。
- 预览健康检查回滚：恢复 `docker-compose.preview.yml` 前端 healthcheck 原参数。
- 预览环境回滚仅操作 `/opt/gain-preview`，不要触碰生产目录 `/opt/gain-project`。
