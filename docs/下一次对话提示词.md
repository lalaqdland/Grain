# 下一次对话提示词

## 1. 文档维护规则（短）

- 每次开发会话结束必须更新本文件。
- 本文件只保留**当前真实状态**和**下一步可执行任务**。
- 若状态有变更（接口、部署、测试结果），同步更新：
  - `docs/API文档.md`
  - `docs/开发日志.md`
  - `docs/status/*.md`

---

## 2. 当前真实状态（v3.1.2-observability）

- 代码分支：`dev`
- 本轮已完成：
  - `POST /api/v1/export` 失败事件已支持内存聚合统计
  - 新增 `GET /api/v1/export/stats`（支持 `window_minutes/doc_id/top_n`）
  - `POST /api/v1/rewrite` 新增 `diagnostics.marian`
  - `GET /api/v1/info` 新增 `runtime.marian` 依赖探测与状态输出
  - Mutagen 持续热同步已恢复并验证（`Watching for changes`）
  - dev 预览已重建到当前实现，`/api/health` 返回 `3.1.2`
- 自动化验证：
  - `python -m pytest -q` -> `19 passed`
  - `frontend npm run build` -> 通过
  - `frontend npm run e2e` -> `1 passed`
- 预览环境：
  - `https://dev.grain.capootech.com/api/health` 正常
  - `https://dev.grain.capootech.com/api/v1/info` 可见 `runtime.marian`
  - 上传 -> 改写 -> 无效导出 -> 统计查询闭环已通过

---

## 3. 当前阻塞/风险

1. `GET /api/v1/export/stats` 为内存态，服务重启会清空历史统计。
2. 预览前端使用 `next dev`，容器 healthcheck（5s）在首轮编译期间可能显示 `unhealthy`。
3. Marian 当前默认关闭且依赖未安装，状态将持续为 `disabled/dependency_ready=false`。

---

## 4. 下一步优先任务（P0/P1/P2）

### P0（必须优先）

1. 预览环境健康信号稳定化
   - 调整 `docker-compose.preview.yml` 前端 healthcheck 超时/重试参数，避免 `next dev` 首次编译误报。

### P1（重要）

1. 导出统计持久化方案评估与落地
   - 将 `failed_ids` 统计从内存态升级为可持久化（SQLite/日志聚合二选一）。
2. Marian 依赖可用性落地
   - 在预览环境安装 `transformers/torch/sentencepiece` 并验证 `status=enabled` 路径。

### P2（可延后）

1. 前端交互细化
   - 选区无效提示分级（跨段、空选区、快照漂移）。
2. 文档结构继续收敛
   - 继续减少重复说明，保留单一事实源。

---

## 5. 验收清单与回滚提示

### 开发完成后最小验收

- [ ] `python -m pytest -q` 全通过
- [ ] `frontend npm run build` 通过
- [ ] `frontend npm run e2e` 通过
- [ ] dev 环境 `api/health` 正常
- [ ] `api/v1/export/stats` 可查询并返回预期统计

### 回滚提示（仅预览环境）

- 优先回滚 `/opt/gain-preview` 到上一个稳定包并 `docker compose up -d --build`。
- 不要对生产目录 `/opt/gain-project` 执行任何回滚操作。
