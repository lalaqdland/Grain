# 下一次对话提示词

## 1. 文档维护规则（短）

- 每次开发会话结束必须更新本文件。
- 本文件只保留**当前真实状态**和**下一步可执行任务**。
- 若状态有变更（接口、部署、测试结果），同步更新：
  - `docs/API文档.md`
  - `docs/开发日志.md`
  - `docs/status/*.md`

---

## 2. 当前真实状态（v3.1.3）

- 代码分支：`dev`
- 本轮已完成：
  - `GET /api/v1/export/stats` 已从内存聚合升级为 SQLite 持久化
  - `GET /api/v1/export/stats` 参数 `window_minutes` 上限从 `1440` 提升到 `43200`
  - `POST /api/v1/export` 失败统计写入异常已降级处理（不影响 `400 + failed_ids` 主流程）
  - `backend/config.py` 新增 `export_stats_db_path`（环境变量：`EXPORT_STATS_DB_PATH`）
  - Marian 默认启用（`use_marian_mt=true`）
  - `docker-compose.yml` / `docker-compose.preview.yml` / `docker-compose.server.yml` backend 均显式 `USE_MARIAN_MT=true`
  - `backend/requirements.txt` 正式纳入 Marian 依赖：`transformers/torch/sentencepiece/sacremoses`
  - `docker-compose.preview.yml` 前端 healthcheck 已调整为高容错（`timeout=10s`、`retries=8`、`start_period=180s`）
  - 新增回归：Marian `used` / `generation_failed` API 路径、`window_minutes` 30天边界
- 自动化验证：
  - `python -m pytest -q` -> `23 passed`
  - `frontend npm run build` -> 通过
  - `frontend npm run e2e` -> `1 passed`

---

## 3. 当前阻塞/风险

1. 本轮仅完成本地改造与本地回归，尚未执行远端 `dev` 目录重建与线上验收。
2. Marian 全环境启用后，首次模型下载与冷启动时延会上升（依赖镜像拉取与模型缓存）。
3. `export_stats.db` 采用长期保留策略，需后续补充数据增长监控与归档策略。

---

## 4. 下一步优先任务（P0/P1/P2）

### P0（必须优先）

1. 执行 dev 预览环境部署与验收（仅 `/opt/gain-preview`）
   - `docker compose -f docker-compose.preview.yml up -d --build --force-recreate`
   - 验证 `api/health`、`api/v1/info(runtime.marian)`、`export/stats` 持久化（重启后不丢失）

### P1（重要）

1. 持久化运维策略完善
   - 为 `export_stats.db` 增加体积监控、备份/归档策略说明
2. Marian 运行态监控
   - 记录首轮模型加载耗时与失败率，补充告警阈值建议

### P2（可延后）

1. 前端交互细化
   - 选区无效提示分级（跨段、空选区、快照漂移）
2. 文档结构继续收敛
   - 减少重复说明，保持单一事实源

---

## 5. 验收清单与回滚提示

### 开发完成后最小验收

- [x] `python -m pytest -q` 全通过
- [x] `frontend npm run build` 通过
- [x] `frontend npm run e2e` 通过
- [ ] dev 环境 `api/health` 正常（待部署后验证）
- [ ] `api/v1/export/stats` 在重启后可查询历史统计（待部署后验证）
- [ ] `api/v1/info` 显示 Marian `enabled=true` 且依赖就绪（待部署后验证）

### 回滚提示

- 关闭 Marian：将 `USE_MARIAN_MT=false` 后重建相关容器。
- 统计层回滚：恢复 `backend/app/core/export_observability.py` 为内存实现（接口不变）。
- 预览健康检查回滚：恢复 `docker-compose.preview.yml` 前端 healthcheck 原参数。
- 预览环境回滚仅操作 `/opt/gain-preview`，不要触碰生产目录 `/opt/gain-project`。
